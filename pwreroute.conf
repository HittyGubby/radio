# ~/.config/pipewire/pipewire-pulse.conf.d/radio-route.conf

pulse.rules = [
  {
    matches = [
      { application.process.binary = "your-binary-name" }
    ]
    actions = {
      update-props = {
        node.target = "radio"
      }
    }
  }
]
